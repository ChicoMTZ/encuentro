<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Title</title>
</head>
<body>
<h1>Home de la plataforma</h1>

{{ user.profile }}

        {% if not user.is_authenticated %}
            <a href="{% url 'New_User' %}" class="btn btn-lg btn-register">Register</a>
            <a href="{% url 'auth_login' %}" class="btn btn-lg btn-register">Login</a>
        {% else %}
            {% if not user.profile %}
            <a href="{% url 'New_Profile' %}" class="btn btn-lg btn-register">Create Profile</a>
                {% endif %}

            <a href="{% url 'View_Profile' user.profile.pk %}" class="btn btn-lg btn-register">View</a>
            <a href="{% url 'Create_Event' user.profile.pk %}" class="btn btn-lg btn-register">Create Event</a>
            <a href="{% url 'edit_profile' user.profile.pk %}" class="btn btn-lg btn-register">Edit Profile</a>
            <a href="{% url 'salir' %}" class="btn btn-lg btn-register">salir</a>

        {% endif %}
</body>
</html>